name: Java TestNG Automation
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 17  # Updated to match project requirements
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'  # Changed from 11 to 17 as per project specs
        
    - name: Cache Maven packages
      uses: actions/cache@v4
      with:
        path: |
          ~/.m2/repository
          target
        key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
        restore-keys: |
          ${{ runner.os }}-maven-
    
    - name: Verify testing.xml exists
      run: |
        if [ -f "testing.xml" ]; then
          echo "testing.xml found"
          cat testing.xml
        else
          echo "testing.xml not found in root directory"
          find . -name "testing.xml" -type f
        fi
        
    - name: Run tests with Maven
      run: mvn clean test -X  # Added -X for debug logging
      
    - name: Archive test reports
      uses: actions/upload-artifact@v4
      if: always()  # Upload reports even if tests fail
      with:
        name: test-reports
        path: |
          target/surefire-reports
          src/test/java/utils/reports/
        if-no-files-found: warn
